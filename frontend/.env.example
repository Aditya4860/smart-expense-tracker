# ─────────────────────────────────────────────────────────────────────────────
# Smart Expense Tracker — Frontend Environment Variables
# Copy to .env or .env.local to override defaults during development/build.
# ─────────────────────────────────────────────────────────────────────────────

# Backend API base URL
# Default in development: http://localhost:8000/api/v1
# In production (via Nginx proxy): /api/v1
VITE_API_URL=http://localhost:8000/api/v1
